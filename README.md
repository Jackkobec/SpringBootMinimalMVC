## Quick start: 
### Build and run project:
```Maven
mvm clean
mvn install
mvn spring-boot:run
```

## OR:
### Build project:
```Maven
mvm clean
mvn install
```
### Run project:
```Shell
java -jar target/SpringBootMinimalMVC.war
```
